# Shared Homebrew formulae (CLI tools that must come from brew, not nixpkgs).
# Prefer home-manager/modules/packages/ for CLI tools; use this only when the
# nixpkgs version is missing or broken on darwin.
[ ]
