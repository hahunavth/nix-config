# Homebrew taps for every macOS host. The default homebrew/core and
# homebrew/cask need no tap.
#
# Kept as a file anyway so there is an obvious place for one, and somewhere to
# record the tap that is deliberately absent: the Atlassian tap, whose formulae
# are broken. The Plugin SDK comes from pkgs/atlassian-plugin-sdk instead.
[
  "stablyai/orca" # Orca — IDE for orchestrating AI coding agents (stablyai/orca/orca cask)
]
